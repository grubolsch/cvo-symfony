{% extends 'base.html.twig' %}


{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    <p>Hallo {{ name }}</p>

    <p><a href="{{ path('app_ping') }}">Tweede pagina</a></p>

    {% if ingelogd %}
        <p>Je bent ingelogd.</p>
    {% else %}
        <p>Je bent NIET ingelogd.</p>
    {% endif %}

    {% if isAdult %}

    {% endif %}

    <ul>
    {% for hobby in hobbies %}
        <li>{{ hobby }}</li>
    {% else %}
        <li>Geen hobbies gevonden</li>
    {% endfor %}
    </ul>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/Users/keelen/Desktop/www/school/symfony_leren/src/Controller/HomepageController.php'|file_link(0) }}">src/Controller/HomepageController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/Users/keelen/Desktop/www/school/symfony_leren/templates/homepage/index.html.twig'|file_link(0) }}">templates/homepage/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}

{% block footer %}
    Mijn speciale footer!
{% endblock %}
