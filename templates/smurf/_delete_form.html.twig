<form method="post" action="{{ path('app_smurf_delete', {'id': smurf.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ smurf.id) }}">
    <button class="btn">Delete</button>
</form>
